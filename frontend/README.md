# Frontend prototype (optional)

This folder can contain a React + TypeScript app using React Flow for visualization.
For the take-home, a minimal prototype can be added later. Current focus is backend correctness.

